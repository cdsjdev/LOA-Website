version: 1
frontend:
  phases:
    build:
      commands: []
  artifacts:
    # Static site — no build step. Serve the repo root as-is.
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
